seed: 42

data:
  image_size: 32
  sequence_length: 6
  num_sequences: 500

model:
  action_dim: 4
  hidden_dim: 32

train:
  batch_size: 16
  epochs: 5
  learning_rate: 0.001

output:
  save_dir: outputs